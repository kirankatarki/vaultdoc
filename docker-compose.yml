services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    volumes:
      - ./backend:/app
      - techtonic_cache:/root/.cache/Tectonic
    environment:
      - UV_LINK_MODE=copy
    restart: unless-stopped

volumes:
  techtonic_cache:
